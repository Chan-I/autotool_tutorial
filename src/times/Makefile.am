AUTOMAKE_OPTIONS = subdir-objects

libtimeslibdir = $(libdir)/libtimes
libtimeslib_PROGRAMS = libtimes.so
libtimes_so_SOURCES = times.c
libtimes_so_LDFLAGS = -shared

AM_CFLAGS = -I $(top_srcdir)/include
